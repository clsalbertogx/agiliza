// Domain Contracts — Public API contract types.
// This is the single source of truth for the frontend ↔ backend contract.
// Frontend imports via @agiliza/shared (which re-exports from here).
// Backend domain imports directly from here.

export * from './enums';
export * from './interfaces';
