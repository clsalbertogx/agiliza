/**
 * Single source of truth for the frontend version, kept in sync with the
 * current git tag (v0.12.0). NEXT_PUBLIC version injection is out of scope —
 * bump this literal on release.
 */
export const VERSION = '0.12.0';
